# ⚠ STALE / NOT-FLASHED (annotated 2026-07-27, persona sync PULL-VERIFY). This file is NOT what any
# board is flashed with. flash-board.sh (r2-composer/orchestrator/bench) selects the COMPOSER CATALOGUE
# template BY CARRIER: DFR -> catalogue/boards/esp32-s3-dfr1195/templates/partitions.csv (which DECLARES
# r2cfg data 0x06 @0x11000/0xF000 — persona is in a DECLARED region, NOT the gap this file describes);
# XIAO -> esp32-s3-xiao-wio-sx1262/templates/partitions.csv (phy_init@0x11000, persona in an unclaimed
# gap). SOURCE OF TRUTH = the composer catalogue, per carrier. Canon target (persona ruling pt7): phy_init
# STAYS @0x11000/0x1000 + a DEDICATED declared persona region @0x12000 (NOT r2cfg) — new to all current
# tables. This file kept for the raw-offset rationale below only. Do NOT flash from it.
#
# DFR1195 partition table — 2-OTA. Flashed at the 0x8000 default (espflash --partition-table).
# Works with the ESP-IDF OTA-capable bootloader (composer-staged) which honors otadata.
# NOTE: the PERSONA enrolment blob (real-TG #20) is NOT a partition entry — espflash's
# esp-idf-part rejects custom data subtypes (panics on 0x40). Persona is a RESERVED RAW
# OFFSET at 0x12000 (size 0x2000) in the free gap between phy_init (ends 0x12000) and
# ota_0 (0x20000): composer esptool-writes the CBOR blob to 0x12000; firmware esp-storage-
# reads 0x12000 raw by offset. No partition lookup needed for a fixed-offset raw region.
# CARRIER BOARD-PROFILE (9-board): a 1-byte flag @0x13000 — 0x00 = no-screen (XIAO-S3),
# else/0xFF(erased) = screen (DFR1195). Firmware gates the ST7735S init+render on it so
# ONE binary runs on both. Also a raw write-bin offset (in the same free gap), not a
# partition entry. composer write-bins 0x00 there on each XIAO.
# Name,    Type, SubType,  Offset,   Size
nvs,       data, nvs,      0x9000,   0x6000
otadata,   data, ota,      0xf000,   0x2000
phy_init,  data, phy,      0x11000,  0x1000
ota_0,     app,  ota_0,    0x20000,  0x1E0000
ota_1,     app,  ota_1,    0x200000, 0x1E0000
