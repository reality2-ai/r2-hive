# Rust build artefacts
/target
**/*.rs.bk

# Editor / OS noise
.idea/
.vscode/
.DS_Store

# Ad-hoc test fixtures the daemon writes at runtime when XDG paths
# resolve under the workspace root in dev shells
/r2-data/

# Compiled deploy binaries — use GitHub releases / git-lfs, not git history (supervisor/Roy)
prebuilt/

# Python bytecode cache (carrier-bridge) — generated, never track
__pycache__/
*.pyc

# local scrub provenance (never public)
.r2-local/
.cursor/
.windsurf/
.clinerules/
.opencode/
.github/copilot-instructions.md
